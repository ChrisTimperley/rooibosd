(jbuild_version 1)

(executable
 ((libraries (core_kernel opium rooibos))
  (name rooibosd)))

(alias
 ((name DEFAULT)
  (deps (rooibosd.exe))))

(install
 ((section bin)
  (files ((rooibosd.exe as rooibosd)))))
